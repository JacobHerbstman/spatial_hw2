# setup_environment

Checks local Julia availability and writes a short environment report.

This task does not install packages automatically; it reports status and points to the project environment at `tasks/_lib/cdp_julia`.
