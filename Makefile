.PHONY: all check clean

all:
	@echo "spatial_hw2 bootstrap is set up. No pipeline targets yet."

check:
	@echo "Repository bootstrap check: root files present, replication folder ignored."

clean:
	@echo "Nothing to clean yet."
